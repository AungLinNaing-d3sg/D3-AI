// NOTE (QA): This file was a scratch/investigation file created while
// evaluating hydration-warning test strategies for src/app/layout.tsx. Its
// real content was moved to __tests__/RootLayout.test.tsx. The QA sandbox's
// shell tool blocks `rm`/`mv` on this path even though it is inside the
// allowed working directory, so this file could not be deleted or renamed
// programmatically. Please delete this file manually
// (`rm __tests__/scratch.test.tsx`) before merging — see QA report for
// details. Left as a trivial passing test in the meantime so it doesn't
// fail the suite.
test.skip("scratch file — superseded by RootLayout.test.tsx, safe to delete", () => {
  expect(true).toBe(true);
});
